# Copy to .env to switch the anonymous metrics on. Without these the game makes
# no network calls at all and the settings screen shows no privacy switch.
#
# The address printed by `wrangler deploy`, see analytics/README.md.
VITE_METRICS_URL=

# Which build a measurement came from, so a bad release shows up in the panel.
VITE_BUILD_ID=dev
